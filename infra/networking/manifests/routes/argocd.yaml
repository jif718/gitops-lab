apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argocd-server
  namespace: argocd
spec:
  parentRefs:
  - name: main-gateway
    namespace: nginx-gateway
    sectionName: https
  hostnames:
  - argocd.local
  rules:
  - backendRefs:
    - name: argocd-server
      port: 80          # Service port 80; backend pod listens on HTTP
