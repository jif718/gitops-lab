apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: networking
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: http://linux03.local:3000/admin/gitops-lab.git
    targetRevision: main
    path: infra/networking/manifests
    directory:
      recurse: true            # include routes/ subdir
  destination:
    server: https://kubernetes.default.svc
    # only applies to resources that omit metadata.namespace (nginx-gateway / metallb-system set their own)
    namespace: nginx-gateway
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false   # namespaces already exist; do not let ArgoCD create them

