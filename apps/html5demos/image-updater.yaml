apiVersion: argocd-image-updater.argoproj.io/v1alpha1
kind: ImageUpdater
metadata:
  name: html5demos-updater
  namespace: argocd
spec:
  writeBackConfig:
    method: "git"
    gitConfig:
      # Gitea repo (note: http, not https, unless you fronted it with TLS)
      repository: "http://linux03.local:3000/admin/html5demos-chart.git"
      branch: "main"
      writeBackTarget: "helmvalues:/values.yaml"
  applicationRefs:
    - namePattern: "html5demos"
      images:
        - alias: "html5demos"
          imageName: "linux02.local/myapp/html5demos"
          commonUpdateSettings:
            updateStrategy: "newest-build"
            allowTags: "regexp:^[0-9]{14}-[0-9a-f]{7}$"
          manifestTargets:
            helm:
              name: "image.repository"
              tag: "image.tag"
